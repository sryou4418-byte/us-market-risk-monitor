# v4.1.0 구현 설계 · 2026-09-09

사용자 지시: 인수인계대로 다음 빌드를 만들고 GitHub 업데이트.
프레임워크는 Streamlit을 유지하며 실제 5개 화면을 재구성한다.

## 화면 구조 시안

| 화면 | 첫 화면 | 이어지는 정보와 이동 |
| --- | --- | --- |
| 홈 | 큰 위험 요약 + 구조적 위험/급변신호 | 관측일·계산 가능 범위 → 핵심 지표 행 → 상세 이동 |
| 위험지수 | 최종 점수와 기본 점수·하한 형성 과정 | 6개 구성요소 비교 행 → 세 축 감지 근거 → 추이·계산 설명 |
| 시장 상태 | 영역별 신호 수와 자료 확인 수 | 그룹별 지표 행: 값·상태·근거·관측일 |
| 시장맵 | 비중/등락 시각화 | 검색 가능한 종목 목록으로 작은 타일의 정보 접근 |
| 뉴스 | 카테고리 필터와 검색 | 매체·시각 → 제목의 세로 목록 |

데스크톱: 상단 브랜드 / 가로 탐색 → 넓은 요약 + 보조 신호 패널.
모바일: 상단 브랜드 → 위험 요약 → 신호 2개 → 지표 목록 / 고정 하단 탐색.
320px에서 텍스트 축소 대신 줄바꿈한다. 모든 화면은 같은 상태 배지와 글자·간격을 사용한다.
홈에 6개 카드·모든 지표·긴 설명을 반복하지 않고 상세 페이지로 연결한다.

## 공식 지침 → 구체적인 적용

2026-09-09 공식 페이지 확인. 아래는 프로젝트 채택 기준이며 공식 인증 주장이 아니다.

- [Apple UI Design](https://developer.apple.com/design/tips/): 읽기 순서와 조작 인접성 → 대표 요약과 근거 버튼, 시스템 글꼴, 주요 버튼 44 CSS px 목표. Apple의 pt를 CSS px 의무값으로 해석하지 않는다.
- [W3C Reflow](https://www.w3.org/WAI/WCAG22/Understanding/reflow.html): 320 CSS px 재배치 → 한 열 지표, 줄바꿈, 시장맵 목록 대안. 실제 브라우저 폭과 확대를 별도 검증한다.
- [Microsoft 아키텍처 원칙](https://learn.microsoft.com/en-us/dotnet/architecture/modern-web-apps-azure/architectural-principles): 변경 책임 분리 → data_service / risk_engine / risk_service / presentation / 각 view / ui_shell.

## 데이터 및 상태 계약

- data_service.MarketStore: 공급원과 디스크 캐시. 인스턴스에 경로를 고정하여 작업 중 환경/세션이 달라져도 갱신 대상이 바뀌지 않는다.
- risk_engine: 기존 합성 위험 계산을 AST 동일하게 분리. 네트워크·Streamlit 의존 없음. 시장 상태 엔진과 구분 유지.
- risk_service: 데이터·CAPE·기준일을 받아 현재/이전 계산을 조립. 점수 재해석 없음.
- presentation.Metric: 유한 값 또는 None, 단위, 상태, 관측일, 근거, 발표시점(None 허용). 관측일을 발표일로 표시하지 않는다.
- 표시용 자료 확인 수는 계산 가능한 구성요소 수이며 통계적 신뢰도나 원시 자료 완전성 비율이 아니다.
- 선택·테마·검색은 세션, 공유 시장자료는 디스크 캐시와 기존 TTL/중복 억제 유지. 갱신 실패를 최신 완료로 표시하지 않는다.
- 뉴스와 시장맵은 코어 데이터 없이 진입. 뉴스는 점수와 무관.

최종 디자인 수용 여부는 사용자 평가로 별도 기록한다. 빌드 성공만으로 수용 완료로 바꾸지 않는다.
