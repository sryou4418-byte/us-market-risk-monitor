# v4.1.0 개발 인수인계

작성: 2026-09-09. 기준 main: 5fcd142.
사용자 지시: “인수인계대로 다음빌드 만들고 깃허브에 업데이트”.
상태: 새 빌드 구현·자동 검증 및 로컬 실제 화면 확인 완료. 사용자 디자인 수용은 아직 평가 전.

## 구현

- 5개 화면의 정보 구조 재구성. 홈은 대표 점수 + 두 신호 패널과 주요 지표 행. 상세는 점수 형성 과정과 근거 탐색. 시장 상태는 관측일이 있는 행. 시장맵은 검색 가능한 목록, 뉴스는 제목 검색을 추가했다.
- ui_shell / ui_theme: 데스크톱 가로 탐색, 모바일 고정 하단 탐색, 공통 색·글자·상태·조작 규칙. 사용하지 않는 옛 CSS와 화면 렌더링 제거.
- app_version.py 단일 버전 기준. 브랜드·하단·빌드 이름이 같은 버전을 사용한다. 기존 배포 프로세스의 모듈이 남아 있으면 프로세스 재시작 후 확인해야 한다.
- 메뉴 이동 시 상단과 제목 초점 복원. 자체 정적 JavaScript만 st.html로 실행하며 부모 프레임·외부 스크립트·사용자 입력 주입은 없다. 이에 따라 Streamlit 최소 버전은 1.63이다.
- data_service.MarketStore: 공급원과 캐시 경로를 인스턴스에 고정. UI 의존 없이 기존 공급원·TTL·실패 시 캐시 보존을 유지한다.
- data_contract: 날짜 인덱스·숫자·유한값·중복·정렬 계약. 결측 월을 채우거나 발표시각을 추정하지 않는다. 빈 물가 시계열의 날짜 인덱스 문제를 정규화 경계에서 처리한다.
- risk_engine: 금융 함수 38개를 그대로 추출. 이전 main 대비 AST 해시 동일. market_engine / market_primitives는 원본 유지. 두 엔진의 의미가 다른 규칙을 통합하지 않았다.
- risk_service: 기준일과 입력을 받아 기존 현재/이전 계산 흐름 조립.
- presentation: 값·단위·상태·관측일·근거·발표시점 미확인(None) 표시 계약. 상세 누락 점수를 0점으로 바꾸던 처리와 구조적 신호 튜플 숫자 노출 제거.
- 계산 가능한 영역/요인/축 개수 표시. 자료 완전성 또는 신뢰도 비율이 아니다. 일부 이력의 충분성·발표일을 전부 검사했다는 의미가 아니다.
- streamlit_app.py: 2,371줄 → 123줄. 라우팅·진행상태·세션·화면 조합 담당.

## 검증

[QA_v4.1.0.md](QA_v4.1.0.md), tests/golden_risk.json 참조.
기존 43개 + 계약/기준일/결측/역사 계산/이전 산식 회귀 8개 = 51개 자동 테스트 통과.
모바일 320/390px, 가로 844px, 데스크톱 1280px와 라이트/다크를 실제 브라우저에서 확인했다.
200% 실제 브라우저 확대는 미확인. 640px 콘텐츠 폭 검증과 구분한다.

## 요구사항별 상태

| ID | 구현 근거 | 상태 |
| --- | --- | --- |
| ARCH-01 | data_service / risk_engine / risk_service / 각 view / ui_shell | 구현·검증 |
| ARCH-02 | data_contract / presentation, 결측·날짜·비유한값 테스트 | 구현·검증, 발표시점 공급원 계약 연구 지속 |
| ARCH-03 | 진입부 중복 함수/옛 UI 제거, 두 시장 엔진은 의미 구분 유지 | 구현·회귀 검증 |
| UX-01 | DESIGN_v4.1.0 화면 역할·홈→근거→상세 | 구현 |
| UX-02 | 전후 스크린샷, 탐색·대표 요약·지표 행 재설계 | 구현·로컬 화면 확인, 사용자 평가 대기 |
| UX-03 | ui_theme.css 공통 체계, 지도 기하 CSS만 별도 | 구현 |
| UX-04 | 모바일·가로·데스크톱·테마·상단/초점·버튼 크기 | 부분 검증, 실제 200% 확대/실기기 잔여 |
| GUIDE-01 | DESIGN 문서의 공식 출처→결정→파일→검증 | 적용 기록 완료, 규격 인증 아님 |
| QA-01 | 38개 금융 함수 AST 동일 + 정상/스트레스/결측/짧은 이력 고정 회귀 | 통과 |
| QA-02 | 배포 기록은 아래 항목과 GitHub 커밋에서 별도 관리 | 서비스 확인 별도, 사용자 수용 미확정 |

## 보류 및 다음 조치

1. 실제 iPhone/Android 및 브라우저 200% 확대, 보조기술 전체 사용성 검증. 현재 브라우저 환경의 실제 확대 조작은 적용 여부를 확인하지 못했다.
2. 실환경 인터넷 최초 로딩과 충분한 표본의 전환 p95 측정. 기록된 5회 측정은 로컬 캐시 및 합성 자료 조건의 탐색 측정이다.
3. CAPE/Breadth 공급원, 월간 고용자료 누락, 지표 발표시점과 관측일 정합성, 장기 금융 검증은 기존 미완료 금융 연구로 유지. UI 개편으로 판정 기준을 임의 조정하지 않았다.
4. 사용자 디자인 평가 후 추가 보완. 코드·배포 완료를 디자인 최종 승인으로 기록하지 않는다.

## 배포

GitHub 업데이트와 실제 서비스 확인 결과는 이 문서의 후속 기록으로 남긴다.
빌드 파일은 prepare_release.py로 생성하며 SHA256SUMS.json을 함께 제공한다.

### 2026-09-09 배포 확인

- GitHub main에 v4.1.0 소스 반영: `34a94a681cc52c17290be54f3a2bd36f3766ae62`.
- 원격 Git 트리와 51개 테스트를 통과한 로컬 빌드의 트리가 동일함을 확인했다.
- Streamlit 실제 서비스에서 상단과 하단 v4.1.0 표시, 홈 → 위험지수 → 시장 상태 → 시장맵 → 뉴스 탐색을 확인했다. 시장맵은 200종목, 뉴스는 144개 기사를 표시했다. 이는 확인 당시의 상태이며 자료의 정확성 인증은 아니다.
- 서비스: https://us-market-risk-monitor-4jbpgfv2xztkvcahynp6sd.streamlit.app/
- 자동 배포로 반영되어 별도 재시작은 필요하지 않았다. 사용자 디자인 수용 및 위의 잔여 검증은 미완료로 유지한다.
