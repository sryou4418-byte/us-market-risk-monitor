# v3.49.0 검증 결과

작성: 2026-09-08. 비교 기준: GitHub main의 v3.48.1.

## 기능 검증

자동 테스트 38개 통과. 뉴스 화면의 별도 새로고침 버튼 제거, 시장상태 펼침 설명 제거, 원자재 선물 명칭, CPI 달력 월 계산과 부분 표시, 최근 월 누락 탐지를 확인했습니다. 기존 작업 중복 방지, 병렬 수집, 캐시 보존, 실제 앱 5개 페이지 실행 및 시장현황 판정 테스트도 유지됩니다.

Streamlit AppTest와 네트워크 mock을 사용했습니다. Windows/아이폰 실기기 또는 실제 브라우저 픽셀 검증을 의미하지 않습니다. Python 문법 검사 통과.

## 성능 측정

| 대상 | 이전/첫 계산 | 개선/캐시 사용 | 조건 |
|---|---:|---:|---|
| FRED CSV 변환 | 0.5341초 | 0.0085초 | 동일 1,253행, 5회 중앙값, 결과 동일 확인 |
| 11개 요청 처리 | 0.5517초 | 0.1521초 | 요청마다 0.05초 모의 지연, 순차→최대 4개 병렬 |
| 시장현황 평가 | 0.1076초 | 0.0036초 | 기존 12개 원자료, 첫 계산과 동일 자료 캐시 5회 중앙값 |

이는 구성요소 측정입니다. 전체 앱이 같은 비율로 빨라진다는 뜻은 아닙니다. 인터넷 응답시간, 사용자 PC, 디스크 속도, 캐시 유무에 따라 달라집니다. 실제 외부 서버 전체 로딩 벤치마크는 수행하지 않았습니다.

## 계산 범위

종합위험지수 산식·가중치·구조적 위험·급변·진행 중 스트레스 하한은 변경하지 않았습니다. 시장상태 임계값도 변경하지 않았습니다. 변경된 계산은 시장상태의 CPI·근원 CPI·근원 PCE 결측 처리뿐입니다. 뉴스 분류 모듈은 위험 계산 모듈을 가져오거나 호출하지 않습니다.

## v3.49.0 변경 확인

- 뉴스는 별도 버튼 없이 저장 자료를 먼저 표시하고 필요할 때 자동 갱신합니다.
- 시장상태의 37개 지표 카드는 유지하되 지표별 긴 펼침 설명은 화면에서 제거했습니다. 판정 규칙과 교차확인 로직은 코드와 문서에 보존됩니다.
- CPI 계열은 정확한 달력 기준월을 사용하며 전년비 또는 3개월 연율 중 하나만 계산 가능해도 그 결과를 표시합니다.
- 최근 16개월 캐시의 중간 월 누락은 전체 FRED 재수집 대상으로 판정합니다.
- 금·은·구리·WTI는 각각 COMEX 또는 WTI 선물임을 제목에 표시합니다.

## 테스트 환경에서 함께 수정한 문제

- Windows 한국어 기본 인코딩에서도 소스 기반 테스트가 실행되도록 UTF-8 읽기를 명시했습니다.
- 시장상태 카드 제목이 HTML escape된 형태로 안전하게 출력되는 현재 UI에 맞춰 테스트를 갱신했습니다.

## 한계와 후속 검증

이번 검증은 Streamlit AppTest와 네트워크 mock 중심입니다. 실제 FRED 서버의 일시 장애 상태, Windows 브라우저 및 아이폰 Safari 픽셀·터치 검증은 별도로 남아 있습니다. CPI 재수집이 실패하면 이전 캐시를 유지하며 화면에는 남아 있는 자료 범위 안에서 상태를 표시합니다.

작업 중복 방지는 단일 프로세스 범위입니다. 여러 서버 프로세스가 같은 캐시 폴더를 공유하는 배포는 별도 분산 잠금이 필요합니다. 지표 임계값 보정 및 발표 당시 자료를 이용한 백테스트는 이번 작업 범위에 포함하지 않았습니다.

