> 2026-09-09 최신 구현과 배포 확인: [v4.1.0 인수인계](DEVELOPMENT_v4.1.0.md). 아래 내용은 **다음 업데이트를 위한 지침**이며, 이번 기록 갱신으로 코드·빌드·배포를 변경하지 않는다.

## 다음 업데이트 시작 지침 (2026-09-09)

1. 다음 개발을 시작할 때는 `main`의 최신 상태와 v4.1.0 인수인계를 먼저 읽는다. 이 문서 안의 v4.0.0-dev 점검 수치와 “미완료” 표기는 당시 기준의 역사 기록이므로 현재 코드 상태로 간주하지 않는다.
2. 먼저 실제 사용자가 체감하는 흐름을 점검한다: 홈 → 위험지수 근거 → 시장 상태·시장맵·뉴스. 이전 선택, 검색어, 테마, 스크롤 위치가 의도대로 유지되는지 확인한다.
3. 금융 산식·가중치·임계값을 UI 작업 과정에서 임의로 바꾸지 않는다. 수집·정규화·계산·표시의 경계를 변경할 때는 기존 위험 계산 회귀 검증을 함께 통과시킨다.
4. 우선 보완할 검증은 실제 iPhone/Android, 브라우저 200% 확대, 키보드·보조기술 사용성, 실환경 최초 로딩 및 충분한 표본의 전환 성능이다. 로컬 합성 자료에서의 결과를 실서비스 품질 보증으로 표현하지 않는다.
5. CAPE/Breadth 공급원, 월간 자료 누락, 관측일·발표시점 정합성, 장기 금융 검증은 열린 과제로 유지한다. 보류할 경우 이유와 다음 조치를 인수인계에 기록한다.
6. 구현 전에는 이 문서의 추적표를 현재 상태로 다시 평가하고, 구현 후에는 파일·커밋·검증 근거를 v4.1.0 인수인계 형식으로 남긴다. 사용자 디자인 평가는 자동 테스트나 배포 성공과 별도로 받는다.
7. 시장맵은 단순히 도면 내부 크기만 조절하지 말고, 도면 컨테이너 자체가 화면에서 차지하는 비율을 키운다. 데스크톱과 모바일의 첫 화면에서 핵심 시장 흐름을 가능한 한 스크롤 없이 파악할 수 있게 상단 설명·여백·조작부와 도면 높이를 함께 재배치한다.

### 시장맵 화면 점유율 개선 완료 기준

- 일반적인 데스크톱 화면에서는 상단 탐색과 최소한의 설명을 포함해 시장맵의 핵심 영역이 첫 화면 안에 들어와야 한다.
- 모바일에서는 브라우저 상단·하단 영역을 고려한 실제 가용 높이를 사용하고, 시장맵을 보기 위해 불필요한 세로 스크롤을 요구하지 않아야 한다.
- 종목 수나 타일 크기만 줄여 맞추지 않는다. 시장맵 컨테이너의 폭·높이, 주변 여백, 안내문과 필터 배치를 함께 조정한다.
- 지도 가독성과 터치 가능 크기를 유지하며 320px·390px 모바일, 가로 화면, 1280px 이상 데스크톱에서 실제 화면을 비교한다.
- 상세 설명이나 긴 안내는 첫 화면 아래 또는 접을 수 있는 영역으로 옮길 수 있지만, 자료 출처·범례·갱신 상태는 쉽게 확인할 수 있어야 한다.


# 다음 버전 필수 반영 — 설계 구조와 디자인 철학

작성일: 2026-09-08
상태: 사용자 확정 요구사항 / 다음 버전 반영 대상 / 구현·검증 미완료.
이번 요청은 GitHub 기록 갱신이다. 코드 변경·새 빌드·추가 배포를 수행하지 않는다.

## 사용자 지시와 우선순위

사용자: “이것도 다음 버전에 반영하게 깃허브에 올려 다음버전 개발할때 이 내용이 누락되지 않게”
직전 설계 점검과 아래 내용을 다음 버전 개발의 요구사항으로 보존한다.
현재 v4.0.0-dev는 사용자 평가상 기존과 너무 유사하여 전면 개편 목표에 미달한다.
단순 카드 재배치·문구 수정·소폭 스타일 변경으로 다음 개편을 축소하지 않는다.
자동 테스트 통과와 배포 성공을 디자인 목표 충족으로 취급하지 않는다.

## 반드시 읽을 기준

- [최신 UI 개편 방향 및 사용자 평가](UI_REDESIGN_DIRECTION_2026-09-08.md)
- [프로그램 설계 원칙](DESIGN_PRINCIPLES.md)
- [Apple 디자인 적용 지침](APPLE_DESIGN_GUIDE.md)
- [실제 구현·배포·미해결 사항](DEVELOPMENT_v4.0.0-dev.md)

이 문서는 위 문서를 대체하지 않고 다음 작업의 실행·검증 항목을 연결한다.
상충하는 경우 최신 사용자 지시와 최신 평가를 우선하며 과거 완료 기록을 소급해 바꾸지 않는다.
다음 버전 번호와 프레임워크 교체 여부는 미확정이다.

## 1. 제품 목적과 금융 의미 유지

목적: 사용자가 첫 화면에서 미국 시장의 현재 위험을 빠르게 이해하고 필요한 근거를 탐색한다.

| 영역 | 사용자 질문 | 의미 있는 표시 |
| --- | --- | --- |
| 위험지수 | 전반적인 시장 위험 수준은 어느 정도인가? | 0~100 점수와 위험구간 |
| 구조적 위험 | 시장 내부에 어떤 취약성이 누적됐는가? | 엔진 판정, 감지 요인과 개수 |
| 시장 급변신호 | 단기적으로 빠른 악화나 충격이 나타나는가? | 엔진 판정, 감지 축과 변화 |

- 세 축은 화면에서 구분하지만 계산상 완전히 독립적이지 않다. 현재 구조·급변 신호는 최종 위험지수 하한에 영향을 준다. 세 축을 다시 평균·합산해 중복 반영하지 않는다.
- 금리·신용·경기·물가 등의 6개 카테고리는 위험지수 구성요소이며 상위 세 축을 대체하지 않는다.
- 시장 상태 페이지의 개별 경보와 홈 시장 급변신호는 목적·계산이 다르므로 혼동하지 않는다.
- 뉴스·시장맵 등 보조 정보의 역할을 구분한다. 뉴스는 점수 산식에 포함하지 않는다.
- 예시 이미지의 점수·등락·날짜·기사·추이를 실제 데이터로 취급하지 않는다. 엔진에 없는 점수를 UI용으로 만들지 않는다.

## 2. 책임 분리와 입력·출력 계약

| 책임 | 요구사항 |
| --- | --- |
| 수집 | 공급원 변경이 계산·화면으로 퍼지지 않도록 경계를 정한다. |
| 정규화·검증 | 단위, 시간대, 날짜, 관측일과 발표시점, 결측·중복·자료 지연을 명시한다. |
| 계산·판정 | 데이터·기준일·설정을 명시적으로 받고 UI·네트워크 실행 없이 검증 가능하게 만든다. |
| 표시용 결과 | 값·단위·상태·근거·관측일·자료 부족 여부를 일관된 형태로 전달한다. |
| 화면 | 사용자 입력·배치·탐색·차트를 담당한다. 금융 판정을 다시 구현하지 않는다. |
| 상태·캐시 | 사용자 선택은 세션 범위로, 공유 시장자료는 별도 관리한다. 갱신 대상·키·수명·실패 처리를 명시한다. |

화면을 바꿔도 계산값은 같아야 한다. 공급원을 바꿔도 UI 전체를 다시 만들지 않아야 한다.
역할 분리는 파일 수 늘리기나 무조건적인 클래스·서비스 추가가 목적이 아니다.
실제 변경 책임에 맞춰 필요한 만큼 단순한 구조를 사용한다.

## 3. 현재 코드 점검 결과와 정리 대상

점검 기준: 2026-09-08 main 90e15f2. 수치는 당시 상태이며 다음 착수 시 다시 확인한다.

- streamlit_app.py 2,371줄에 데이터 수집·캐시·위험 계산·신호 판정·해석·뉴스·시장맵·화면 책임이 혼재한다.
- streamlit_app.py와 market_primitives.py에 같은 이름의 최상위 함수 24개가 있고, 19개는 AST 기준 내용도 동일했다.
- 공통 ui_theme.css와 앱 내부 CSS가 공존한다. 사용처·우선순위를 확인하고 공통 규칙으로 정리한다.
- 화면별 직접 가공과 해석 때문에 동일 값의 표시·결측·판정이 달라질 여지가 있다. 결과 전달 계약을 적용한다.
- 여러 버전 문서가 누적되어 최신 요구 누락 위험이 있다. README와 인수인계의 최신 안내를 유지한다.

이미 분리된 market_engine/market_primitives, market_loader/loading_service,
market_view, dashboard_view, ui_shell/ui_theme를 평가해 활용한다.
캐시 우선 표시, 백그라운드 갱신, 중복 작업 억제, 선택 상태 보존은 유지할 기반이다.
동일 이름 함수를 사용처 확인 없이 삭제하거나 목적이 다른 규칙을 억지로 통합하지 않는다.
위험지수 엔진과 시장 상태 엔진의 의미 차이를 유지하며 금융 임계값·가중치를 임의로 변경하지 않는다.

## 4. 디자인 철학을 전체 UI로 구현

- 정보 위계: 현재 상태 → 주요 위험 요인 → 상세 근거.
- 단순함: 홈에는 판단에 필요한 정보를 두고 상세는 자연스럽게 탐색한다.
- 일관성: 카드·버튼·상태·단위·아이콘·차트 표현을 공통 규칙으로 관리한다.
- 유연함: 모바일 우선으로 설계하고 데스크톱에는 동일 정보 구조를 공간에 맞게 펼친다.
- 사용자 주도권: 상세를 보고 돌아와도 선택과 맥락을 보존한다.
- 정보의 정직성: 자료 없음·지연·실패를 정상이나 0점으로 보이게 하지 않는다.
- 완성도: 글자·정렬·여백·터치·키보드 초점·라이트/다크·전환·로딩·오류 상태까지 확인한다.

사용자가 제시한 밝은 iOS 계열 예시의 대표 요약, 간결한 세 축 카드,
정돈된 지표 행과 차트, 모바일/데스크톱의 일관된 구성을 시각적 방향으로 적용한다.
미확정 픽셀 사양이라는 이유로 예시 방향을 무시하지 않는다.
흰 배경·파란 포인트·둥근 카드만 적용하고 디자인 철학 구현을 완료했다고 하지 않는다.

## 5. 대기업·공식 가이드 적용 방법

Apple, Microsoft, W3C, 웹 성능 자료 등 기존 문서의 출처는 조사 출발점이다.
추가 조사·채택 시 공식 자료를 확인하고 다음 연결을 기록한다.

공식 지침 → 우리 앱의 문제 → 설계 결정 → 적용 코드/화면 → 검증 결과.

출처·확인일·채택 이유·검증 방법을 남기고 미검증 규격 준수를 주장하지 않는다.
여러 회사의 서로 다른 스타일을 무작정 섞지 말고 우리 앱의 일관된 기준으로 정리한다.
지침 조사 또는 링크 나열 자체를 실제 적용 완료로 간주하지 않는다.

## 6. 다음 버전 진행 순서

1. 최신 main과 위 기준 문서를 읽고 작업계획에 아래 추적표를 포함한다.
2. 홈·위험지수 상세·시장 상태·시장맵·뉴스의 역할과 주요 이동 흐름을 정리한다.
3. 데이터/계산/표시 경계와 결과 계약을 설계하고 중복 코드 사용처를 점검한다.
4. 예시를 바탕으로 전체 화면 시안과 공통 디자인 규칙을 구체화한다.
5. 홈→상세 탐색 흐름을 먼저 완성한 뒤 같은 규칙을 다른 화면으로 확장한다.
6. 금융 계산 회귀와 실제 UI 사용성을 별도로 검증한다.
7. 인수인계에 요구사항별 구현·검증·보류 이유와 남은 작업을 갱신한다.

## 7. 누락 방지 추적표

다음 개발자는 항목을 작업계획으로 옮기고 완료 시 파일/커밋과 검증 근거를 기록한다.
아래는 전부 미완료인 다음 버전 작업이며, 이 기록으로 구현 완료 표시하지 않는다.

| ID | 필수 항목 | 완료 판단 근거 | 상태 |
| --- | --- | --- | --- |
| ARCH-01 | 수집·계산·표시·상태 책임 정리 | 의존 관계와 변경 경계, 관련 코드 | 미완료 |
| ARCH-02 | 표시용 결과 및 결측·시점 계약 | 일관된 반환 형식과 경계/실패 검증 | 미완료 |
| ARCH-03 | 중복 함수·스타일 정리 | 사용처 확인, 제거/유지 이유, 회귀 검증 | 미완료 |
| UX-01 | 전체 정보 구조·탐색 재설계 | 화면별 역할과 홈→근거→상세 흐름 | 미완료 |
| UX-02 | 예시 방향을 반영한 대폭 UI 변화 | 시안과 기존/변경 실제 화면 비교 | 미완료 |
| UX-03 | 공통 디자인 규칙 | 글자·간격·상태색·카드·조작·차트 일관성 | 미완료 |
| UX-04 | 반응형·접근성·전환 | 모바일 세로/가로, 데스크톱, 테마, 확대, 초점, 실제 반응 | 미완료 |
| GUIDE-01 | 공식 지침을 구체적 결정으로 연결 | 출처→문제→결정→구현→검증 기록 | 미완료 |
| QA-01 | 세 축 의미 및 계산 보존 | 고정 입력 회귀, 중복 반영 없음, 결측 검증 | 미완료 |
| QA-02 | 서비스 적용과 사용자 목표 구분 | 배포 커밋/실제 화면/사용자 평가 별도 기록 | 미완료 |

기존 버전 표기 불일치·상세 해석의 튜플 노출, 축별 자료 충족률,
CAPE/Breadth·월간 자료 누락·발표시점·장기 금융 검증 등 기존 미완료 항목도 유지한다.
우선순위를 이유로 보류하는 항목은 사유와 다음 조치를 명시하며 조용히 누락하지 않는다.
