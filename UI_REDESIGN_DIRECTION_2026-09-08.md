# 향후 UI 전면 개편 방향 — 인수인계

기록일: 2026-09-08
상태: 방향 확정 / 미구현 / 사용자 개발 시작 지시 대기

## 현재 지시와 작업 범위

사용자는 UI와 코드 구조의 대대적인 개편 방향을 제시한 뒤, “지금 개발하란건 아냐”라고 명확히 정정했다.
최종 요청은 해당 내용을 GitHub에 기록으로만 남기는 것이다.
따라서 이 문서는 개발 착수 지시가 아니다. 사용자가 별도로 개발 시작을 요청하기 전까지 이 계획을 근거로 코드 수정, 리팩터링, 새 빌드 제작, 배포 또는 서비스 재시작을 진행하지 않는다.
이번 기록 작업은 문서 추가만 수행하며 앱 코드와 버전을 변경하지 않는다.

## 배경과 목표

사용자가 가장 중요하게 보는 것은 시각적으로 읽기 편한 화면과 부드러운 화면 전환이다.
[Apple 디자인 지침](APPLE_DESIGN_GUIDE.md)과 [소프트웨어 설계 원칙](DESIGN_PRINCIPLES.md)을 향후 개편 판단의 기준으로 사용한다.
GitHub 최신 기록과 코드를 기준으로 계획을 구체화한다.

## 향후 개편에서 허용된 방향

- UI는 기존과 완전히 달라져도 좋다. 기존 구성을 유지해야 한다는 제약은 없다.
- 기존의 좋은 구성과 기능은 평가해서 활용할 수 있다.
- 정보 우선순위, 글자와 여백, 카드 크기, 메뉴 구조를 전체적으로 재검토한다.
- 모바일, 세로 화면, 가로 화면에서 읽기 좋은 배치와 크기를 검토한다.
- 화면 전환의 반응, 대기 표시, 화면 흔들림과 선택 상태 유지를 검토한다.
- 사용하지 않는 사이드바 카테고리의 정리를 검토한다. 제거할 구체적인 목록은 아직 확정하지 않았다.
- 코드도 수립한 소프트웨어 설계 원칙에 맞춰 역할 분리와 재배열을 할 수 있다.

이는 개발 착수 후 검토할 범위이며, 지금 구현을 승인한 것으로 해석하지 않는다.
UI 개편 방향만으로 금융 계산식이나 위험 판정 기준 변경까지 확정된 것은 아니다.

## 아직 결정하지 않은 사항

최종 화면 구성, 디자인 시안, 삭제할 메뉴 목록, 모듈 분리 범위, 구체적인 크기·전환 시간, 다음 버전 번호와 일정은 미정이다.
가이드 문서의 수치는 실제 화면에서 검증할 후보값으로 취급한다.

## 개발 시작 요청 이후의 확인 항목

1. GitHub 최신 코드와 인수인계를 다시 확인한다.
2. 정보 배치와 주요 사용자 흐름을 설계한다.
3. 변경 범위에 맞는 구현과 검증을 진행한다.
4. 모바일 세로·가로, 넓은 화면, 라이트·다크, 글자 확대 및 주요 화면 이동을 확인한다.
5. 계산 관련 회귀 여부, 실제 전환 반응과 미확인 항목을 기록한다.
6. 코드 구현, 자동 테스트, GitHub 반영, 서비스 배포, 실제 화면 검증 상태를 구분해 인수인계한다.

## 이번 기록의 완료 범위

향후 개편 방향과 개발 보류 조건을 문서화했다.
이 기록은 기존 빌드의 구현·테스트·배포 검증 상태를 변경하지 않는다.

---

## v4.0 화면 구조 시안 논의 추가 기록

기록일: 2026-09-08
상태: 예시 이미지 검토 중 / 최종 UI 미확정 / 구현 지시 아님

### 이번 대화에서 확인된 방향

- 사용자는 v4.0 화면 구조를 먼저 이미지 목업으로 검토하고 싶어 한다.
- 여러 예시 이미지 중, 모바일 메인 화면과 세부지표 화면, 데스크톱 화면이 함께 보이는 밝고 정돈된 iOS 계열 레이아웃을 긍정적으로 평가했다.
- 현재 생성된 이미지는 어디까지나 **예시 이미지 / 컨셉 목업**이며, 실제 구현 화면이나 확정 디자인이 아니다.
- 따라서 Codex는 이 이미지를 픽셀 단위 사양으로 해석하지 말고, 정보 우선순위와 레이아웃 방향을 이해하기 위한 참고 자료로만 취급한다.

### 매우 중요한 판별 구조 정정

v4.0 메인 화면을 설계할 때 시장을 `금리/경기/변동성/신용` 같은 카테고리 점수 묶음으로 설명하는 구조로 오해하면 안 된다.
사용자가 명확히 정정한 핵심 판별 구조는 아래 **3개 축**이다.

1. **위험지수**
   - 현재 시장의 전반적인 위험 수준을 수치화하는 핵심 지표.
   - 기존 종합위험지수의 역할을 담당한다.

2. **구조적 위험**
   - 금리, 신용, 경기 등 시장 내부에 누적되는 중장기적/구조적 위험 요인을 판단하는 영역.
   - 단기 가격 급변과는 별개로 시장 체질이 얼마나 취약한지 보여주는 축으로 이해한다.

3. **시장 급변신호**
   - 단기간의 비정상적 가격 움직임이나 급격한 변동을 빠르게 감지하는 영역.
   - 구조적 위험과 달리 단기 충격/이상 움직임 탐지 성격이 강하다.

이 세 영역은 단순한 시각적 카테고리가 아니라 **시장 판단 로직의 상위 구조**다.
따라서 v4.0 홈 화면에서는 이 3개 축이 서로 다른 역할을 가진다는 점이 명확히 드러나야 한다.

### v4.0 메인 화면에서 우선 검토할 표현 방식

- 최상단 또는 가장 큰 영역에는 사용자가 한눈에 현재 시장 상태를 파악할 수 있는 대표 요약을 배치한다.
- 그 아래 또는 인접 영역에 `위험지수 / 구조적 위험 / 시장 급변신호`를 3개의 핵심 카드 또는 3분할 구조로 표현하는 방안을 우선 검토한다.
- 세 카드가 모두 같은 의미의 단순 점수처럼 보이면 안 된다. 각 카드에는 서로 다른 역할을 설명하는 짧은 보조 문구가 필요하다.
- 예: 위험지수는 현재 위험 수준, 구조적 위험은 누적 위험 요인, 시장 급변신호는 단기 충격 감지라는 차이가 즉시 읽혀야 한다.
- 세부지표 화면에서는 이 3개 축을 기준으로 하위 지표를 탐색할 수 있는 구조를 검토한다.
- 뉴스와 시장 코멘트는 보조 정보이며, 3개 판별 축보다 시각적 우선순위가 높아지지 않게 한다.

### 디자인 톤

- 밝고 정돈된 iOS 스타일.
- 흰색 기반, 넉넉한 여백, 둥근 카드, 약한 그림자.
- 파랑 계열을 기본 포인트로 사용하되 위험 상태는 주황/빨강 등 최소한의 상태색으로 구분한다.
- 숫자와 상태 문구가 먼저 읽히고, 설명과 세부 데이터는 그 다음 단계에서 읽히도록 한다.
- 모바일에서 먼저 구조를 정리한 뒤 데스크톱은 같은 정보 구조를 더 넓게 펼치는 방식으로 검토한다.

### Codex가 다음 대화에서 이어갈 때

1. 이 문서와 최신 GitHub 코드를 먼저 확인한다.
2. v4.0의 핵심 판별 구조가 반드시 `위험지수 / 구조적 위험 / 시장 급변신호` 3개라는 점을 전제로 한다.
3. 이전에 생성된 이미지 시안은 참고용이며 확정 사양이 아니라는 점을 유지한다.
4. 사용자가 구현을 명시적으로 지시하기 전에는 코드 수정, 리팩터링, 빌드, 배포를 진행하지 않는다.
5. 구현 시작 전에는 먼저 홈 화면의 정보 구조와 3개 축의 시각적 표현 방식을 사용자와 확정한다.

## 개발 착수 — 2026-09-08

사용자의 “깃허브 가면 여태 작업목록 저장한거 있어 확인하고 개발시작” 지시로 개발을 시작했다.
이전 개발 대기 상태는 해소됐으며, 예시 이미지가 최종 사양이 아니라는 조건은 유지한다.
세 축 중심 홈의 검토 가능한 첫 구현과 자동 검증을 진행했다.
전체 v4.0 완성·최종 디자인 확정·배포 완료를 의미하지 않는다.
구현 내용, 검증 결과 및 남은 작업은 [v4.0 개발 인수인계](DEVELOPMENT_v4.0.0-dev.md)를 참고한다.
