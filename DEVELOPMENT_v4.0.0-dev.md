> 2026-09-09 최신 구현: [v4.1.0 인수인계](DEVELOPMENT_v4.1.0.md). 아래 이전 시점 상태와 구분하며, 요구사항별 현재 상태는 최신 인수인계를 참조합니다.

# v4.0.0-dev — 홈 화면 개편 1차

## 다음 버전 필수 반영 — 설계·디자인 점검

2026-09-08 사용자가 직전 설계 구조·디자인 철학 점검 전체를 다음 버전에 반영하고 누락 없이 인수인계하도록 요청했다.
[NEXT_VERSION_REQUIREMENTS.md](NEXT_VERSION_REQUIREMENTS.md)를 개발 시작 전 읽고, ARCH/UX/GUIDE/QA 추적표를 작업계획과 완료 보고에 연결한다.
코드 역할 분리, 결과 계약, 중복 정리, 예시 기반 전면 UI 개편, 공식 지침 적용과 실제 검증을 포함한다.
현재 상태는 요구사항 확정·미구현이며 이번 변경은 문서만 수정한다.


## 최신 평가 — 전면 개편 목표 미달 (2026-09-08)

사용자는 이번 빌드가 기존과 너무 비슷하여 대대적인 개편 방향에 부합하지 않는다고 명확히 평가했다.
**1차 구현 및 배포 사실은 유지하되, 사용자 요구 충족·디자인 승인·전면 개편 완료로 간주하지 않는다.**

다음 개편은 Apple 등 대기업의 공식 디자인·프로그램 설계 지침을 실제 화면과 코드에 적용하고,
정보 구조·탐색·시각 체계·반응형·화면 전환을 포함해 UI를 대폭 재설계해야 한다.
기존 카드 정리와 소폭 스타일 변경으로 범위를 축소한 접근은 사용자 요구에 미달했다.
예시 이미지는 시각적 방향의 기준이며, 미확정 시안이라는 이유로 무시하지 않는다.
위험지수·구조적 위험·시장 급변신호의 세 축과 실제 계산 의미는 유지한다.

상세 요구와 예시의 해석, 다음 검증 기준은
[UI 개편 방향의 최신 사용자 평가](UI_REDESIGN_DIRECTION_2026-09-08.md)를 먼저 읽는다.
이번 요청은 GitHub 인수인계 수정만이며 앱 코드·버전·빌드 변경 및 추가 배포는 수행하지 않는다.
아래 구현/검증 내역은 과거 사실 기록이다.


작성일: 2026-09-08. 기준 main: 05ff15277374148256a9c99942f814d277db580f.
작업 브랜치: codex/v4-home. 상태: 1차 구현 및 자동 검증 완료 / 실제 화면 QA 미완료.

## 착수 근거와 범위

사용자가 “깃허브 가면 여태 작업목록 저장한거 있어 확인하고 개발시작”이라고 지시했다.
UI_REDESIGN_DIRECTION_2026-09-08.md의 개발 시작 대기 조건은 이 지시로 해소됐다.
기존 컨셉 이미지는 확정 사양이 아니다. 기록된 세 축을 기준으로 검토 가능한 첫 구현을 만들었다.
이 문서는 전체 v4.0 완성이나 디자인 최종 확정을 선언하지 않는다.

## 구현 완료

- 홈 상단: 시장 요약 → 위험지수 / 구조적 위험 / 시장 급변신호.
- 위험지수만 0~100 점수로 표시. 나머지는 기존 엔진의 상태와 감지 개수로 표시.
- 급변신호 개수만으로 경고색을 부여하던 표시를 엔진 판정 상태와 일치시켰다.
  예를 들어 2개 신호가 있어도 엔진이 관찰로 판단하면 관찰 색상을 유지한다.
- 반복되는 신호 설명을 접고, 구조적 위험 및 급변 요인을 각각 펼쳐 확인한다.
- 위험지수 산출 근거 버튼은 기존 세션 보존 콜백으로 상세 화면에 연결한다.
- 6개 카테고리의 제목을 위험지수 구성요소로 바꿔 상위 세 축과 구분한다.
- S&P500 관측일을 명시하고 모든 지표의 공통 기준일로 오해하지 않도록 표시한다.
- dashboard_view.py에 홈 표시 책임을 분리. 외부 문자열은 HTML escape 처리한다.
- ui_theme.css에 본문 폭별 1열 / 점수 카드 + 2열 / 3열 배치를 추가했다.
- 버전 표시는 4.0.0-dev. 금융 엔진, 임계값, 가중치, 데이터 공급원 변경 없음.

## 검증

- 기존 unittest 40개 통과 (17.167초, Python 3.12 / Streamlit 1.63.0).
- 새 홈 표시 테스트 3개 통과: 판정 상태 유지, 결측 표시, HTML escape.
- 홈 상세 버튼 왕복 검증을 기존 캐시 기반 통합 테스트에 추가하고 해당 테스트 재실행 통과.
  데이터 재수집 없이 dashboard → risk → dashboard 이동 및 기존 페이지 왕복을 검증했다.
- 기존 streamlit_app.py 최상위 함수 146개의 AST가 기준 커밋과 동일함을 확인했다.
- Python 컴파일 및 git diff --check 통과.
- 브라우저에서 개발 서버 접근 시 net::ERR_BLOCKED_BY_CLIENT 발생.
  실제 렌더링/모바일 세로·가로/라이트·다크/200% 확대/click-to-paint는 미검증.
- 정식 서비스 배포 및 실행파일 빌드 수행 안 함. 개발 브랜치로 전달한다.

## 남은 작업

1. 실제 화면으로 홈 구성과 세 축의 표현을 검토하고 확정한다.
2. 모바일·확대·테마·초점·화면 전환을 실제 브라우저로 검증한다.
3. 나머지 페이지 개편과 더 큰 모듈 분리는 이 첫 단계 검토 후 이어간다.
4. 기존 엔진은 일부 입력이 없어도 확보된 값으로 신호를 판정한다.
   홈에 신호 없음이 전체 자료 확보를 의미하지 않는다고 표시했으며,
   축별 자료 충족률을 제공하는 입력 계약 개선은 후속 작업이다.
5. CAPE/Breadth, 월간 고용자료 누락, 발표시점 정합성, 장기 검증 등
   이전 인수인계의 금융 연구 미완료 항목을 유지한다.


## main 반영 및 실제 서비스 확인 — 2026-09-08

- 사용자 요청으로 PR #1을 main에 병합했다. 병합 커밋: 5485c80b230a5fa68164d236a6af0ab352d96c6a.
- 서비스 주소: https://us-market-risk-monitor-4jbpgfv2xztkvcahynp6sd.streamlit.app/
- 실제 브라우저에서 새 홈의 세 축 카드, 감지 요인 펼치기 조작부, 위험지수 산출 근거 버튼을 확인했다.
- 산출 근거 버튼 클릭 후 위험지수 상세 화면으로 이동함을 확인했다.
- 데스크톱 스크린샷에서 세 카드가 가로로 정상 배치됨을 확인했다. 모바일/200% 확대/다크모드 검증은 아직 미완료다.
- 발견된 미해결 사항: 홈 본문과 하단은 새 코드(4.0.0-dev)인데 상단/사이드바 버전은 v3.50.0이다. 브라우저 새로고침 후에도 지속된다. 이전 ui_shell 모듈이 서버에 남았을 가능성이 있으나 원인은 확정하지 않았다. 실제 앱 재시작 또는 배포 모듈 갱신 상태 점검이 필요하다.
- 따라서 새 홈 서비스 적용은 확인 완료이나, 버전 표기 일치 및 전체 UI QA 완료로 간주하지 않는다.
- 추가 발견: 기존 위험지수 상세 해석에 구조적 신호 튜플의 숫자가 그대로 노출된다. 별도 표시 정리 대상이며 이번 홈 개편의 계산 변경은 아니다.
