# v3.50.0 UI 구성 및 검증 기록

기준: GitHub main 23698b46c42dd83ca224ff9959024043847d3c5b. 작성일 2026-09-08.
상태: 코드 구현 완료 / 자동 검증 완료 / 실제 브라우저 시각·배포 검증 미완료.

## 목적과 발견한 문제

사용자가 우선한 가치는 시각적으로 편안한 화면과 부드러운 페이지 전환이다.
v3.49.0 코드를 확인하면 세로 화면에서 위험 카테고리를 6열로 강제하고
일부 글자를 8~11px까지 축소한다. 시장 상태 설명은 10.3px이다.
페이지·테마·뉴스 분류는 일반 URL 링크로 이동해 Streamlit 세션의 연속성을 잃을 수 있다.
이전 앱 실측에서는 재배포 이후 버전 표시와 시장 모듈이 엇갈렸으며 서버 재시작 후 정상화됐다.
이는 과거 v3.49.0의 확인 결과이며 이번 UI의 시각 검증으로 간주하지 않는다.

## 적용한 배치 방향

화면의 가로·세로 방향 자체보다 사이드바를 제외한 실제 본문 너비를 기준으로 한다.
브라우저 확대나 메뉴 개폐로 공간이 줄어도 글자 축소보다 카드 재배치를 우선한다.

| 실제 본문 폭 | 설명이 있는 시장 카드 | 대시보드 상단 | 6대 카테고리 | 뉴스 분류 |
|---|---|---|---|---|
| 620px 미만 | 1열 | 1열 | 2열 | 2열 |
| 620~999px | 2열 | 종합점수 전체 폭 + 보조 2열 | 3열 | 3열 |
| 1000px 이상 | 3열 | 3열 | 3열 | 5열 |

예: 모바일 세로 390px는 좌우 16px 여백을 둔 약 358px 본문,
가로 844px에서 사이드바가 닫혀 있으면 좌우 24px를 둔 약 796px 본문을 사용한다.
가로모드여도 사이드바가 열려 공간이 부족하면 1열로 돌아간다.
최대 본문 박스 폭은 1280px. 카드 간격은 기본 16px, 작은 위험 카테고리는 10px.
카드 안쪽 여백은 모바일 16px, 넓은 화면 20px. 높이는 내용에 따라 늘어난다.
설명 카드 최소 높이 160px, 위험 카테고리 164px를 기준으로 하되 고정 높이로 글자를 자르지 않는다.

## 글자·색·조작

- 주요 설명 14px, 본문 16px, 뉴스 제목 17px, 섹션 제목 20px.
- 지표값 24~30px, 종합점수 44~60px, 상태 배지 13px.
- 숫자는 tabular-nums로 자리 폭을 맞춰 값 변경 시 흔들림을 줄인다.
- 설명 행간 1.6. 긴 지표명은 줄바꿈을 허용하고 뉴스 출처도 잘라 숨기지 않는다.
- 주요 메뉴·분류·갱신 버튼의 최소 높이는 44px. 키보드 초점 테두리를 표시한다.
- 회색 글자와 카드 배경을 라이트/다크 공통 색상 변수로 관리한다.
- 하락 파란색·상승 붉은색을 유지하고 상태는 텍스트를 병기한다.
- 미구현 데이터·리포트·설정·도움말 메뉴를 제거한다. 다크 모드는 유지한다.
- 모바일 메뉴는 기본 사이드바의 접기/펼치기를 사용한다.
- 기존 시장맵의 전체 맵 축소 원칙은 유지한다. 작은 타일의 폰트는 일반 본문 최소 크기 규칙의 예외이며 후속 실기기 점검 대상이다.

## 전환 구조와 실제로 측정한 범위

메뉴, 테마, 뉴스 카테고리는 Streamlit 버튼 콜백으로 상태를 갱신한다.
URL의 view/theme/news_category도 유지하여 직접 링크를 열 수 있다.
메뉴 이동은 refresh 일회성 매개변수를 지우며 사용자 데이터 갱신을 불필요하게 반복하지 않는다.
테마용 부모 문서 접근 스크립트를 제거하고 CSS를 서버 응답에 포함했다.
버튼 색 변화는 120ms이며 prefers-reduced-motion에서는 애니메이션·전환을 끈다.
페이지 전체 fade나 slide를 추가해 실제 로딩을 지연시키지 않는다.
서버 rerun 자체를 없앤 것은 아니며 외부 수집 완료까지의 시간은 네트워크에 좌우된다.

로컬 Streamlit 1.63.0 AppTest, 저장된 합성 뉴스·시장맵, requests.get 호출 금지 환경에서
동작별 10회 측정했다. 브라우저 클릭→픽셀 표시 시간이나 FPS가 아니다.

| 동작 | 중앙값 | 최대값 |
|---|---:|---:|
| 뉴스 → 시장맵 | 118.59ms | 173.55ms |
| 시장맵 → 뉴스 | 116.05ms | 129.39ms |
| 뉴스 분류 | 115.66ms | 133.51ms |

원문: ui_transition_benchmark.json. 재현: `python measure_ui.py`.
이전 버전과 동일한 브라우저 환경을 측정하지 않았으므로 개선율이나 FPS를 주장하지 않는다.

## 자동 검증과 남은 확인

자동 테스트 40개. 5개 앱 경로, 같은 세션의 메뉴 왕복, 테마·분류 유지,
분류에 맞는 기사 출력, 예기치 않은 네트워크 호출 없음, 기존 금융 판정 테스트를 포함한다.
기존 market_engine.py/market_primitives.py/market_view.py의 계산·판정 코드 변경 없음.

실제 브라우저 조작은 자동 승인 검토가 사용량 한도로 거부했다.
따라서 이번 버전의 픽셀 배치, 모바일 Safari, 실제 전환 체감 및 배포 후 실행을 확인했다고 표현하지 않는다.
권한과 사용량이 복구되면 360×800, 390×844, 844×390, 768×1024, 1440×900에서
라이트/다크, 메뉴 접기, 200% 확대, 긴 제목, 가로 넘침, 뉴스 왕복을 확인한다.
성능 목표 초안은 저장 캐시가 있는 상태에서 클릭 후 화면 표시 p95 500ms 이내다.
이는 제품 목표이며 이번 검증의 통과 수치가 아니다. 실제 각 경로 20회 이상 측정 후 조정한다.

## 참고한 공식 문서

- [Streamlit Session State](https://docs.streamlit.io/develop/api-reference/caching-and-state/st.session_state)
- [Streamlit 버튼 콜백](https://docs.streamlit.io/develop/concepts/design/buttons)

UI 수치는 이 프로젝트를 위한 설계 선택이며 외부 표준의 강제값이라고 주장하지 않는다.
