# 미국 증시 위험 모니터 v3.50.0 개발 인수인계

작성: 2026-09-08. 기준 저장소: sryou4418-byte/us-market-risk-monitor, main.

## 구현 완료

사용자가 UI 점검·구성 방향 정리를 요청한 뒤 같은 작업을 빌드까지 업데이트하도록 확정했다.
이전 인수인계 32절의 사이드바 정리도 함께 구현했다.

- 미구현 메뉴 제거, 실제 5개 페이지와 테마 전환 유지.
- ui_shell.py: Streamlit 기본 사이드바, 세션을 유지하는 메뉴/테마/뉴스 필터 버튼 콜백.
- ui_theme.css: 본문 폭에 따른 카드 배치, 글자·간격·색·초점·동작 줄이기 규칙.
- streamlit_app.py: 새 화면 틀 연결, 부모 DOM 테마 스크립트 제거, 초기 로딩 중 이동도 버튼 사용.
- tests/test_navigation.py 및 기존 로딩 테스트: 페이지 왕복과 선택 상태 유지 검증.
- measure_ui.py 및 ui_transition_benchmark.json: 로컬 AppTest 상호작용 측정.
- .gitignore: Python 바이트코드, 가상환경, 비밀값 제외. 저장소에 들어 있던 생성 바이트코드는 정리 대상.

레이아웃 규격·수치·근거·미검증 범위는 UI_개선_v3.50.0.md를 함께 읽는다.
위험 산식·가중치·판정 임계값 및 공급원은 이번 UI 작업 범위에서 변경하지 않았다.

## 검증과 완료 범위

40개 자동 테스트 통과. 합성 자료를 사용한 같은 세션의 페이지 왕복과 선택 상태 유지,
네트워크 호출 억제 및 기존 지표 판정 검증을 포함한다.
로컬 캐시 조건의 AppTest 전환 중앙값은 약 116~119ms. 실제 브라우저 렌더링 시간이 아니다.

브라우저 도구가 자동 승인 검토의 사용량 한도로 차단되어 실제 배포 후 확인은 미완료다.
GitHub 반영 완료와 서비스 적용 성공을 구분해서 보고한다. 복구 후 앱 버전·새 메뉴·CPI·다크모드·뉴스·모바일을 확인한다.
필요시 승인된 범위에서 Streamlit 앱 재시작을 진행한다.

## 이어갈 작업

1. 실제 브라우저 세로/가로/데스크톱 및 200% 확대 검증. UI 문서의 테스트 크기 참조.
2. 전환 click-to-paint와 스크롤/초점 동작 점검. AppTest 결과를 브라우저 실측으로 대체하지 않는다.
3. CAPE/Breadth 공급원, 월간 고용자료 누락, 발표 시점 검증 등 이전 미완료 금융 연구는 유지.

GitHub main이 공용 기준점이다. 사용자가 새 아이디어를 말하면 상태와 구현 여부를 명시해 기록한다.
