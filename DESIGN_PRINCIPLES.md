# 프로젝트 설계 원칙 — 학습 및 적용 기준

작성: 2026-09-08. 상태: 기본 원칙 정리 / 향후 개발·리뷰에 적용.
이 문서는 원칙을 기록한 것이며 현재 앱이 모든 기준을 충족한다는 인증이나 검증 결과가 아니다.
사용자의 새 요구와 검증 결과에 따라 수정한다. 이번 작업은 문서 작업이며 앱 빌드를 변경하지 않는다.

## 1. 먼저 사용자의 목적을 정한다

기능을 만들기 전에 “누가 어떤 상황에서 무엇을 알아내거나 하려는가?”를 한 문장으로 적는다.
우리 앱은 첫 화면에서 시장 위험을 빠르게 이해하고, 필요한 사람이 근거를 더 살펴보는 흐름이다.
시각적 편안함과 자연스러운 화면 전환을 중요한 제품 목표로 둔다.
화면마다 모든 정보를 반복하기보다 요약 → 원인 → 상세의 순서로 제공한다.
이는 우리 프로젝트의 제품 설계 기준이다.

## 2. 역할을 나누고 변경의 영향을 줄인다

일반 원칙: 관심사 분리, 단일 책임, 명시적인 의존성, 캡슐화.
화면 표시, 데이터 수집, 금융 계산은 서로 다른 변경 이유를 갖는다.
한 모듈이 한 줄의 일만 해야 한다는 뜻은 아니다. 함께 바뀌는 일을 묶는다.
같은 의미의 규칙은 한 곳에서 관리하되, 숫자가 우연히 같다는 이유로 다른 규칙을 합치지 않는다.
[근거: Microsoft 아키텍처 원칙](https://learn.microsoft.com/en-us/dotnet/architecture/modern-web-apps-azure/architectural-principles)

우리 앱 적용:
- 화면의 글자 크기를 바꿔도 위험점수가 바뀌지 않아야 한다.
- 공급자를 바꿀 때 금융 엔진 전체를 다시 작성하지 않도록 입력 단위·날짜·결측 계약을 명시한다.
- 계산 함수는 가능한 한 데이터와 기준일을 명시적으로 받아 같은 입력에 같은 결과를 반환한다.
- 테마·여백 같은 공통 시각 규칙을 한 곳에서 관리한다.
- UI와 엔진에 같은 금융 임계값을 따로 복사하지 않는다.

## 3. 필요한 만큼 단순하게 만든다

우리 프로젝트의 적용 판단:
실제 문제가 없는 부분에 거대한 구조나 새 프레임워크를 먼저 추가하지 않는다.
현재 규모에서 읽기 쉬운 모듈 분리로 해결할 수 있으면 그 방법부터 검토한다.
추상화는 변경 사례가 생겼을 때 도입하며, 복잡한 방법을 선택하면 그 이유와 대안을 기록한다.
동시에 임시 코드·사용하지 않는 CSS가 쌓이는 것도 단순함을 해치므로 관련 변경 때 정리한다.
모든 원칙을 문자 그대로 적용하는 것보다 유지보수 비용과 사용자 경험을 함께 판단한다.

## 4. 읽기 쉬운 화면은 정보의 우선순위와 재배치에서 시작한다

작은 화면이나 확대 화면에서도 주요 정보와 기능을 잃지 않도록 재배치한다.
WCAG Reflow는 일반적인 세로 스크롤 콘텐츠의 320 CSS px 폭을 기준으로 삼고,
이차원 배치가 본질적인 표·지도 등에는 예외가 있다. 예외를 페이지 전체로 확대하지 않는다.
[근거: W3C Reflow](https://www.w3.org/WAI/WCAG22/Understanding/reflow.html)

우리 앱 적용:
- 열 수를 유지하려고 글자를 계속 줄이지 않는다. 본문에 실제로 남은 폭으로 열 수를 정한다.
- 카드 높이는 긴 제목·설명·확대 글자에 맞춰 늘어나야 한다.
- 제목/값/상태/설명 사이의 시각적 순서를 일정하게 유지한다.
- 모바일 세로·가로, 태블릿, 데스크톱, 확대 화면을 각각 점검한다.
- 14px 설명·16px 본문·24~30px 지표값 등 v3.50.0 수치는 출발점이다.
  모든 사용자에게 통하는 절대 비율이나 WCAG의 의무 폰트 크기로 취급하지 않는다.
- 시장맵은 전체 보기와 작은 종목 상세 접근을 함께 검토한다.

## 5. 접근성을 기본 품질로 취급한다

일반 텍스트 대비는 원칙적으로 4.5:1, WCAG에서 정의한 큰 텍스트는 3:1이 최소 기준이다.
비활성 요소 등 예외가 있으므로 실제 사용 상태에 맞춰 검사한다.
[근거: W3C Contrast Minimum](https://www.w3.org/WAI/WCAG22/Understanding/contrast-minimum.html)

WCAG 2.2 AA의 포인터 대상 최소 크기는 원칙적으로 24×24 CSS px이며
간격·인라인 등의 예외가 있다. 우리 앱의 주요 터치 조작은 더 넉넉한 44×44 CSS px를 목표로 한다.
44px 높이 하나만 충족했다고 전체 접근성을 통과한 것으로 간주하지 않는다.
[근거: W3C Target Size Minimum](https://www.w3.org/WAI/WCAG22/Understanding/target-size-minimum.html)

우리 앱 적용:
- 라이트·다크 각각 글자/배경의 실제 대비를 검사한다.
- 위험 상태를 색만으로 표현하지 않고 텍스트를 병기한다.
- 키보드 이동, 보이는 초점, 메뉴 열기/닫기, 확대 후 조작 가능 여부를 확인한다.
- 중요한 설명은 마우스를 올려야만 볼 수 있는 방식에만 의존하지 않는다.
- 동작 줄이기 설정을 존중한다.

## 6. 부드러움은 입력 반응·대기·화면 안정성을 함께 다룬다

RAIL은 응답, 애니메이션, 유휴 시간, 로딩을 구분해 성능을 살핀다.
짧은 입력 응답과 애니메이션 프레임 시간, 전체 로딩 시간은 서로 다른 측정 대상이다.
[근거: web.dev RAIL](https://web.dev/articles/rail)

우리 앱 적용:
- 클릭이 접수됐는지 빠르게 알려주고, 길어지는 작업은 진행 상태를 표시한다.
- 페이지 이동 때 세션·선택값·사용 가능한 캐시를 보존한다.
- 데이터 수집을 화면의 필수 경로에서 가능한 한 분리한다.
- 로딩 전후 카드 크기와 숫자 자리 폭이 불필요하게 흔들리지 않게 한다.
- 화면 전환마다 큰 애니메이션을 넣어 대기 시간을 늘리지 않는다.
- 실제 클릭부터 화면 표시까지의 시간과 서버 처리 시간을 분리해 기록한다.
- 평균뿐 아니라 느린 경우(p95), 최초 실행, 캐시가 있는 재방문을 따로 측정한다.
- v3.50.0의 AppTest 약 116~119ms는 실제 휴대폰 전환 시간이나 FPS가 아니다.

## 7. 데이터가 없거나 실패했을 때도 의미가 정확해야 한다

다음은 일반적인 오류 처리 개념을 이 금융 모니터에 적용한 프로젝트 원칙이다.
- N/A를 0이나 정상으로 바꾸지 않는다.
- 마지막 정상 자료를 보여줄 때 관측일과 지연·수집 실패 여부를 구분한다.
- 경제지표의 관측 대상 월과 실제 발표 시점은 다를 수 있다.
- 달력 기준 계산에서 누락 행을 건너뛰어 다른 달을 대신 사용하지 않는다.
- 일부 계산만 가능하면 가능한 값과 확인할 수 없는 값을 구분한다.
- 한 공급원 실패 때문에 관련 없는 뉴스나 다른 화면까지 멈추지 않게 한다.
- 원인 확정에 필요한 근거가 없으면 관측과 해석의 한계를 명시한다.
- API 키·인증정보를 코드나 GitHub 문서에 넣지 않는다.

## 8. 완료 여부는 사용자에게 도달한 결과로 구분한다

프로젝트 운영 원칙:
코드 구현, 자동 테스트, GitHub 반영, 서비스 배포, 실제 화면 확인을 각각 기록한다.
GitHub 커밋만 성공한 상태를 실제 배포 화면 검증 완료라고 부르지 않는다.
테스트는 의미 있는 경계값·결측·실패·사용자 동작을 검증하며 구현을 그대로 복사하지 않는다.
시각 변경은 가능하면 실제 렌더링으로 확인하고, 도구가 막히면 미확인 범위를 남긴다.
현재 v3.50.0의 실제 브라우저/모바일/배포 후 QA는 이전 기록대로 미완료이며
이 학습 문서가 그 상태를 변경하지 않는다.

## 이후 작업에서 사용하는 방법

1. 작업 시작 시 이 문서와 최신 인수인계를 읽는다.
2. 해당 변경과 관련된 원칙만 골라 설계 판단에 적용한다.
3. 예상 효과, 확인할 사용자 동작, 실패 시 처리를 정한다.
4. 필요한 검증을 수행하고 실제로 확인한 범위를 기록한다.
5. 원칙을 바꾸는 결정은 이유와 날짜를 남긴다.

이 파일은 GitHub에 보존되는 프로젝트 기억이다.
모델의 가중치 학습이나 대화 밖 영구 메모리에 저장됐다는 의미는 아니다.
새 작업은 저장소 문서를 읽어 맥락을 복원한다.

## 학습 순서

첫 단계: 관심사 분리·단일 책임 → 정보 우선순위·일관성 → 반응형·접근성.
두 번째: 상태 관리·캐시·비동기 처리 → 성능 측정.
세 번째: 데이터 계약·오류 복구 → 테스트·배포 확인.
각 단계마다 우리 앱 사례로 설명하고 필요한 개선만 별도 작업으로 진행한다.

## Apple 디자인 철학 함께 적용하기

2026-09-08 사용자 요청으로 [Apple 디자인 철학 및 UI 적용 지침](APPLE_DESIGN_GUIDE.md)을 추가했다.
UI 작업 시작 시 함께 읽고 정보 위계, 글자·여백, 반응형 배치, 화면 전환을 검토한다.
문서의 수치는 프로젝트 실험 후보이며 Apple의 웹 의무 규격이나 구현 완료 선언이 아니다.

## 소프트웨어 설계 운영 기준 — 2026-09-08 확정

사용자 요청에 따라 다음을 향후 구현과 코드 리뷰의 기본 기준으로 채택한다.
현재 코드가 전부 이 구조라는 선언은 아니다. 관련 기능을 수정할 때 점진적으로 적용한다.

| 원칙 | 프로젝트 적용 기준 |
| --- | --- |
| 관심사 분리·단일 책임 | 데이터 수집, 정규화, 위험 계산, 화면 표시, 상태 관리를 구분한다. 함께 바뀌는 책임은 묶는다. |
| 높은 응집도·낮은 결합도 | 관련 규칙을 같은 모듈에 두고 다른 모듈의 내부 상태에 직접 의존하지 않는다. |
| 명시적 의존성 | 계산에 필요한 데이터·기준일·설정을 인자로 전달한다. 계산 모듈에서 UI나 네트워크를 직접 호출하지 않는 방향으로 개선한다. |
| 필요한 만큼 단순하게 | 현재 요구를 해결하는 가장 작은 읽기 쉬운 구조를 선택한다. 예상만으로 범용 프레임워크나 계층을 늘리지 않는다. |
| 규칙의 단일 관리 | 같은 의미의 임계값·계산 규칙·공통 스타일을 한 곳에서 관리한다. 의미가 다른 유사 코드를 무리하게 합치지 않는다. |
| 입력·출력 계약 | 값의 단위, 날짜·시간대, 필수 필드, 결측, 오류와 반환 형식을 경계에서 명시하고 검사한다. |
| 예측 가능한 상태 | 세션 상태와 공유 캐시의 소유자·수명·초기화 조건을 정한다. 사용자별 선택이 다른 세션에 섞이지 않게 한다. |
| 실패 격리·복구 | 외부 요청의 시간 제한과 제한된 재시도를 정한다. 일부 실패를 해당 영역에 표시하고 나머지 기능을 유지한다. |
| 검증 가능한 변경 | 결정적인 계산은 고정 입력으로 검증하고 경계값·결측·실패·주요 사용자 흐름을 확인한다. |
| 관측·안전한 배포 | 오류와 처리 시간을 비밀정보 없이 기록한다. 배포 대상 커밋, 확인 결과, 되돌릴 기준을 남긴다. |

### 권장 책임 흐름

외부 공급원 → 수집 어댑터 → 정규화·검증 → 계산 → 표시용 결과 → UI.

이것은 책임의 구분이다. 각 단계를 별도 서비스나 클래스로 만들 의무는 없다.
단순한 함수와 모듈로 충분하면 그 구조를 사용한다.
UI는 사용자 입력과 화면 표시를 담당하고, 계산 규칙은 UI 실행 없이 검증할 수 있도록 한다.
SOLID 등의 명칭 자체를 충족하려고 상속·인터페이스를 추가하지 않는다.
교체 가능한 공급자 경계처럼 실제 변경 필요성이 있는 곳에 작은 계약을 둔다.

### 상태와 캐시 세부 기준

- 화면 선택·테마·필터 등 사용자 상태는 세션 범위에 둔다.
- 공유 가능한 시장 데이터와 사용자별 정보를 구분한다.
- 캐시 키에는 결과를 바꾸는 입력을 포함하고 자료 특성에 맞는 만료·갱신 조건을 정한다.
- 수동 갱신의 대상과 범위를 명시하고 화면 이동만으로 불필요한 전체 재수집을 하지 않는다.
- 오래된 자료를 재사용하면 자료 시점과 수집 실패 여부를 표시한다.
- 성능 개선은 측정된 병목부터 진행하며 정확성 검증을 함께 유지한다.

### 매 작업에 적용하는 절차

1. 요구와 완료 조건을 짧게 적는다.
2. 변경할 책임과 영향 범위, 데이터·상태 계약을 확인한다.
3. 가장 단순한 구현을 선택하고 중요한 대안·절충만 기록한다.
4. 변경 위험에 맞는 자동 검증과 실제 사용자 동작 확인을 수행한다.
5. 인수인계에 적용 원칙, 검증 결과, 미확인 항목, 배포 커밋을 남긴다.

작은 스타일 수정에도 별도의 장문 설계서를 요구하지 않는다.
공급자 교체, 계산 규칙, 데이터 형식, 상태 저장 방식처럼 영향이 큰 결정은
인수인계에 날짜·문제·결정·이유·영향·재검토 조건을 기록한다.
원칙의 예외가 필요하면 이유와 확인 방법을 남긴다.

이번 작업은 기준 문서 보강이며 코드 리팩터링이나 새 빌드 배포를 수행한 상태가 아니다.
